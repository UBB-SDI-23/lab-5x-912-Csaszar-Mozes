version: '3.8'

services:
  web:
    build: ./assignment1
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./assignment1/:/usr/src/assignment1/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev